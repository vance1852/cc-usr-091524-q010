server:
  port: 0

spring:
  datasource:
    url: jdbc:h2:mem:equipment_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt-secret: test-secret-test-secret-test-secret-32bytes
  admin-username: admin
  admin-password: admin123
